import{_ as e,o as a,c as n,e as s}from"./app-sfMN51f5.js";const t="/assets/images/inner-join.jpg",i="/assets/images/left-join.jpg",o="/assets/images/right-join.jpg",r="/assets/images/full-join.jpg",l={},d=s(`<h1 id="sql-interview-questions" tabindex="-1"><a class="header-anchor" href="#sql-interview-questions" aria-hidden="true">#</a> SQL Interview Questions:</h1><h2 id="what-is-database" tabindex="-1"><a class="header-anchor" href="#what-is-database" aria-hidden="true">#</a> What is database?</h2><p>A database is an organized collection of data, stored and retrieved digitally from a remote or local computer system.</p><h2 id="what-is-dbms" tabindex="-1"><a class="header-anchor" href="#what-is-dbms" aria-hidden="true">#</a> What is DBMS?</h2><p>DBMS stands for Database Management System.It provides a systematic way to create, retrieve, update and manage data.</p><h2 id="what-is-rdbms-how-is-it-different-from-dbms" tabindex="-1"><a class="header-anchor" href="#what-is-rdbms-how-is-it-different-from-dbms" aria-hidden="true">#</a> What is RDBMS? How is it different from DBMS?</h2><p>RDBMS stands for Relational Database Management System. It is a type of DBMS that stores data in a structured format, using rows and columns. In an RDBMS, data can be related to other data, hence the term &quot;relational&quot;. Tables can be linked by defined relations making it possible to combine data from several tables upon request. The primary difference between an RDBMS and a DBMS is that an RDBMS uses a structure that allows for the identification of and access to data in relation to other pieces of data in the database, while a DBMS does not.</p><h2 id="what-is-sql" tabindex="-1"><a class="header-anchor" href="#what-is-sql" aria-hidden="true">#</a> What is SQL?</h2><p>SQL stands for Structured Query Language. It is the standard language for relational database management systems. It is especially useful in handling organized data comprised of entities (variables) and relations between different entities of the data.</p><h2 id="what-is-the-difference-between-sql-and-mysql" tabindex="-1"><a class="header-anchor" href="#what-is-the-difference-between-sql-and-mysql" aria-hidden="true">#</a> What is the difference between SQL and MySQL?</h2><h3 id="sql" tabindex="-1"><a class="header-anchor" href="#sql" aria-hidden="true">#</a> SQL:</h3><p>a standard language for managing and manipulating relational databases. It can be used to perform tasks such as update data on a database, or retrieve data from a database.</p><h3 id="mysql" tabindex="-1"><a class="header-anchor" href="#mysql" aria-hidden="true">#</a> MySQL:</h3><p>an open-source relational database management system, owned by Oracle. It uses SQL to query the database, but it also includes its own additional extensions and features that are not part of the standard SQL. MySQL is known for its speed, reliability, and ease of use.</p><h2 id="what-are-constraints-in-sql" tabindex="-1"><a class="header-anchor" href="#what-are-constraints-in-sql" aria-hidden="true">#</a> What are Constraints in SQL?</h2><p>Constraints in SQL are rules that are applied to data columns on a table. They are used to limit the type of data that can go into a table. This ensures the accuracy and reliability of the data in the table. If there is any violation between the constraint and the data action, the action is aborted.</p><ul><li><strong>NOT NULL</strong> - Restricts NULL value from being inserted into a column.</li><li><strong>CHECK</strong> - Verifies that all values in a field satisfy a condition.</li><li><strong>DEFAULT</strong> - Provides a default value for a column when none is specified.</li><li><strong>UNIQUE</strong> - Ensures that all values in a column are different.</li><li><strong>INDEX</strong> - Used to create and retrieve data from the database very quickly.</li><li><strong>PRIMARY KEY</strong> - Uniquely identifies each record in a database table. Each table can have one primary key.</li><li><strong>FOREIGN KEY</strong> - Uniquely identifies a row/record in any of the given database table. It is used to prevent actions that would destroy links between tables.</li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> Employees <span class="token punctuation">(</span>
    ID <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>  <span class="token comment">-- NOT NULL</span>
    Name <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
    Age <span class="token keyword">int</span> <span class="token keyword">CHECK</span> <span class="token punctuation">(</span>Age <span class="token operator">&gt;=</span> <span class="token number">18</span><span class="token punctuation">)</span><span class="token punctuation">,</span>  <span class="token comment">-- CHECK</span>
    City <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span> <span class="token keyword">DEFAULT</span> <span class="token string">&#39;Unknown&#39;</span><span class="token punctuation">,</span>  <span class="token comment">-- DEFAULT</span>
    Email <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span> <span class="token keyword">UNIQUE</span><span class="token punctuation">,</span>  <span class="token comment">-- UNIQUE</span>
    <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>ID<span class="token punctuation">)</span><span class="token punctuation">,</span>  <span class="token comment">-- PRIMARY KEY</span>
    DeptID <span class="token keyword">int</span><span class="token punctuation">,</span>
    <span class="token keyword">FOREIGN</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>DeptID<span class="token punctuation">)</span> <span class="token keyword">REFERENCES</span> Departments<span class="token punctuation">(</span>DeptID<span class="token punctuation">)</span>  <span class="token comment">-- FOREIGN KEY</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">CREATE</span> <span class="token keyword">INDEX</span> idx_Employees_Name  <span class="token comment">-- INDEX</span>
<span class="token keyword">ON</span> Employees <span class="token punctuation">(</span>Name<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-is-a-primary-key" tabindex="-1"><a class="header-anchor" href="#what-is-a-primary-key" aria-hidden="true">#</a> What is a Primary Key?</h2><p>The <code>PRIMARY KEY</code> constraint uniquely identifies each row in a table. It must contain UNIQUE values and has an implicit NOT NULL constraint. A table in SQL is strictly restricted to have one and only one primary key, which is comprised of single or multiple fields (columns).</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> Students <span class="token punctuation">(</span>   <span class="token comment">/* Create table with a single field as primary key */</span>
   ID <span class="token keyword">INT</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span>
   Name <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span>
   <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>ID<span class="token punctuation">)</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> Students <span class="token punctuation">(</span>   <span class="token comment">/* Create table with multiple fields as primary key */</span>
   ID <span class="token keyword">INT</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span>
   LastName <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span>
   FirstName <span class="token keyword">VARCHAR</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
   <span class="token keyword">CONSTRAINT</span> PK_Student
   <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>ID<span class="token punctuation">,</span> FirstName<span class="token punctuation">)</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> Students   <span class="token comment">/* Set a column as primary key */</span>
<span class="token keyword">ADD</span> <span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>ID<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> Students   <span class="token comment">/* Set multiple columns as primary key */</span>
<span class="token keyword">ADD</span> <span class="token keyword">CONSTRAINT</span> PK_Student   <span class="token comment">/*Naming a Primary Key*/</span>
<span class="token keyword">PRIMARY</span> <span class="token keyword">KEY</span> <span class="token punctuation">(</span>ID<span class="token punctuation">,</span> FirstName<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>PK_Student</code> is the name of a PRIMARY KEY constraint that is being applied to the ID and FirstName columns of the Students table. The PK_ prefix is a common naming convention indicating that this is a primary key constraint.</p><h2 id="what-is-the-difference-between-primary-key-and-unique" tabindex="-1"><a class="header-anchor" href="#what-is-the-difference-between-primary-key-and-unique" aria-hidden="true">#</a> What is the difference between primary key and unique?</h2><ul><li><strong>NULL Values:</strong> A column with a <code>PRIMARY KEY</code> constraint cannot have NULL values. On the other hand, a column with a <code>UNIQUE</code> constraint can have NULL values, unless it is also constrained by NOT NULL.</li><li><strong>Number of Keys</strong>: A table can have only one <code>PRIMARY KEY</code>. However, a table can have multiple <code>UNIQUE</code> constraints.</li><li><strong>Purpose</strong>: The <code>PRIMARY KEY</code> is used to uniquely identify each record in a table. It is often used as a reference for <code>FOREIGN KEY</code> constraints in other tables. The <code>UNIQUE</code>constraint is used when you want to prevent duplicate values in a column, but it doesn&#39;t necessarily identify the record uniquely.</li></ul><h2 id="what-is-a-foreign-key" tabindex="-1"><a class="header-anchor" href="#what-is-a-foreign-key" aria-hidden="true">#</a> What is a Foreign Key?</h2><p>A <strong>FOREIGN KEY</strong> comprises of single or collection of fields in a table that essentially refers to the <strong>PRIMARY KEY</strong> in another table. Foreign key constraint ensures <strong>referential integrity</strong> in the relation between two tables.</p><h3 id="example-1" tabindex="-1"><a class="header-anchor" href="#example-1" aria-hidden="true">#</a> Example 1:</h3><p>If Table A has a foreign key that points to Table B, then every value of the foreign key in Table A must either be null or be found in the primary key of Table B.</p><h3 id="example-2" tabindex="-1"><a class="header-anchor" href="#example-2" aria-hidden="true">#</a> Example 2:</h3><p>For example, we might have a <code>Users</code> table with a primary key of <code>UserID</code>, and an <code>Orders</code> table with a <code>CustomerID</code> foreign key that references the <code>UserID</code> in the Users table. Even though <code>UserID</code> and <code>CustomerID</code> are different names, the <code>CustomerID</code> is still a foreign key that references the <code>UserID</code> primary key.</p><h2 id="what-is-referential-integrity" tabindex="-1"><a class="header-anchor" href="#what-is-referential-integrity" aria-hidden="true">#</a> What is Referential Integrity?</h2><p>Referential integrity is a property of data which, when satisfied, requires every value of one attribute (column) of a relation (table) to exist as a value of another attribute in a different (or the same) relation.</p><p>Referential integrity rules enforce the following:</p><ul><li>A record that contains a foreign key value cannot be inserted into the table that contains the foreign key unless there is a corresponding value in the referenced primary key.</li><li>A record cannot be deleted from a primary key table if matching records exist in a foreign key table.</li><li>A primary key value cannot be updated if matching records exist in a foreign key table.</li></ul><h2 id="what-is-join" tabindex="-1"><a class="header-anchor" href="#what-is-join" aria-hidden="true">#</a> What is Join?</h2><p>The <strong>SQL Join</strong> clause is used to combine records (rows) from two or more tables in a SQL database based on a related column between the two.</p><h3 id="inner-join" tabindex="-1"><a class="header-anchor" href="#inner-join" aria-hidden="true">#</a> <strong>INNER JOIN</strong>:</h3><figure><img src="`+t+`" alt="inner join" tabindex="0" loading="lazy"><figcaption>inner join</figcaption></figure><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> column_name<span class="token punctuation">(</span>s<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> table1
<span class="token keyword">INNER</span> <span class="token keyword">JOIN</span> table2
<span class="token keyword">ON</span> table1<span class="token punctuation">.</span>column_name <span class="token operator">=</span> table2<span class="token punctuation">.</span>column_name<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns records that have matching values in both tables.</p><h3 id="left-join" tabindex="-1"><a class="header-anchor" href="#left-join" aria-hidden="true">#</a> <strong>LEFT JOIN</strong>:</h3><figure><img src="`+i+`" alt="left join" tabindex="0" loading="lazy"><figcaption>left join</figcaption></figure><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> column_name<span class="token punctuation">(</span>s<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> table1
<span class="token keyword">LEFT</span> <span class="token keyword">JOIN</span> table2
<span class="token keyword">ON</span> table1<span class="token punctuation">.</span>column_name <span class="token operator">=</span> table2<span class="token punctuation">.</span>column_name<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns all records from the left table, and the matched records from the right table. If there is no match, the result is NULL on the right side.</p><h3 id="right-join" tabindex="-1"><a class="header-anchor" href="#right-join" aria-hidden="true">#</a> <strong>RIGHT JOIN</strong>:</h3><figure><img src="`+o+`" alt="right join" tabindex="0" loading="lazy"><figcaption>right join</figcaption></figure><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> column_name<span class="token punctuation">(</span>s<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> table1
<span class="token keyword">RIGHT</span> <span class="token keyword">JOIN</span> table2
<span class="token keyword">ON</span> table1<span class="token punctuation">.</span>column_name <span class="token operator">=</span> table2<span class="token punctuation">.</span>column_name<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns all records from the right table, and the matched records from the left table. If there is no match, the result is NULL on the left side.</p><h3 id="full-join" tabindex="-1"><a class="header-anchor" href="#full-join" aria-hidden="true">#</a> <strong>FULL JOIN</strong>:</h3><figure><img src="`+r+`" alt="full join" tabindex="0" loading="lazy"><figcaption>full join</figcaption></figure><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> column_name<span class="token punctuation">(</span>s<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> table1
<span class="token keyword">FULL</span> <span class="token keyword">JOIN</span> table2
<span class="token keyword">ON</span> table1<span class="token punctuation">.</span>column_name <span class="token operator">=</span> table2<span class="token punctuation">.</span>column_name
<span class="token keyword">WHERE</span> condition<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Returns all records when there is a match in either the left or the right table.</p><h2 id="what-is-a-self-join" tabindex="-1"><a class="header-anchor" href="#what-is-a-self-join" aria-hidden="true">#</a> What is a Self-Join?</h2><p>A self-join in SQL is a way to combine rows of the same table when there&#39;s a match based on some condition. It&#39;s used to combine and compare rows within the same table.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> column_name<span class="token punctuation">(</span>s<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> table1 T1<span class="token punctuation">,</span> table1 T2
<span class="token keyword">WHERE</span> condition<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this example, the <code>Employees</code> table is joined with itself using different aliases (<code>A</code> and <code>B</code>). The query returns a list of employees and their respective managers, where the <code>ManagerID</code> of one row in the table matches the <code>EmployeeID</code> of another row in the same table.</p><h3 id="when-to-use-self-join" tabindex="-1"><a class="header-anchor" href="#when-to-use-self-join" aria-hidden="true">#</a> When to use Self-join?</h3><ul><li><strong>Hierarchical Relationships</strong>:If we have a table that represents hierarchical data, such as employees and managers where both are stored in the same <code>Employees</code> table, a self join can be used to retrieve the relationship between employees and their managers.</li><li><strong>Finding Duplicates</strong>: If we want to find duplicate records in a table, a self join can be used to compare the table to itself and find rows where certain column values match.</li><li><strong>Comparing Rows within a Table</strong>: If we need to compare rows within a table based on some condition, a self join can be used. For example, we might want to find pairs of products that have the same supplier.</li></ul><h2 id="what-is-a-cross-join" tabindex="-1"><a class="header-anchor" href="#what-is-a-cross-join" aria-hidden="true">#</a> What is a Cross-Join?</h2><p>A cross join in SQL, also known as a Cartesian product, is a join operation that produces the combination of every row of the first table with every row of the second table if no WHERE or ON condition is used along with CROSS JOIN. In other words, if table1 has n rows and table2 has m rows, a cross join will result in n*m rows.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> Customers<span class="token punctuation">.</span>CustomerName<span class="token punctuation">,</span> Products<span class="token punctuation">.</span>ProductName
<span class="token keyword">FROM</span> Customers
<span class="token keyword">JOIN</span> Products<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This will return a result set that includes every combination of customer names and product names.</p><h2 id="the-sql-case-expression" tabindex="-1"><a class="header-anchor" href="#the-sql-case-expression" aria-hidden="true">#</a> The SQL CASE Expression</h2><p>The <code>CASE</code> expression goes through conditions and returns a value when the first condition is met (like an if-then-else statement).</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CASE</span>
    <span class="token keyword">WHEN</span> condition1 <span class="token keyword">THEN</span> result1
    <span class="token keyword">WHEN</span> condition2 <span class="token keyword">THEN</span> result2
    <span class="token keyword">WHEN</span> conditionN <span class="token keyword">THEN</span> resultN
    <span class="token keyword">ELSE</span> result
<span class="token keyword">END</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-is-an-index-explain-its-different-types" tabindex="-1"><a class="header-anchor" href="#what-is-an-index-explain-its-different-types" aria-hidden="true">#</a> What is an Index? Explain its different types.</h2><p>An index in SQL is a data structure that improves the speed of data retrieval operations on a database table. It works similarly to an index in a book: just as a book index helps you find information quickly without having to read every page, a database index provides a fast way to look up data without scanning every row in a table.</p><p>Indexes are used to quickly locate data without having to search every row in a database table every time a database table is accessed. They can be created using one or more columns of a database table, providing the basis for both rapid random lookups and efficient access of ordered records.</p><p>while indexes speed up data retrieval, they also slow down data modification operations, such as <code>INSERT</code>, <code>UPDATE</code>, and <code>DELETE</code>. This is because each time data is changed, the index also needs to be updated. Therefore, it&#39;s important to find a good balance and not over-index your database tables.</p><h3 id="clustered-index" tabindex="-1"><a class="header-anchor" href="#clustered-index" aria-hidden="true">#</a> <strong>Clustered Index</strong>:</h3><p>Determines the physical order of data in a table. There can be only one clustered index per table.</p><h3 id="non-clustered-index" tabindex="-1"><a class="header-anchor" href="#non-clustered-index" aria-hidden="true">#</a> <strong>Non-Clustered Index</strong>:</h3><p>Does not sort the physical data inside the table. Instead, it creates a separate object within a table which points back to the original table rows after creating the index. A table can have multiple non-clustered indexes.</p><h3 id="unique-index" tabindex="-1"><a class="header-anchor" href="#unique-index" aria-hidden="true">#</a> <strong>Unique Index</strong>:</h3><p>Unique indexes are indexes that help maintain data integrity by ensuring that no two rows of data in a table have identical key values. Once a unique index has been defined for a table, uniqueness is enforced whenever keys are added or changed within the index.</p><h3 id="non-unique-index" tabindex="-1"><a class="header-anchor" href="#non-unique-index" aria-hidden="true">#</a> <strong>Non-unique Index</strong>:</h3><p>Non-unique indexes, on the other hand, are not used to enforce constraints on the tables with which they are associated. Instead, non-unique indexes are used solely to improve query performance by maintaining a sorted order of data values that are used frequently.</p><h2 id="what-is-data-integrity" tabindex="-1"><a class="header-anchor" href="#what-is-data-integrity" aria-hidden="true">#</a> What is Data Integrity?</h2><p>Data integrity in SQL refers to the accuracy, consistency, and reliability of data stored in a database. It ensures that the data is correct and reliable over its entire lifecycle.</p><h3 id="entity-integrity" tabindex="-1"><a class="header-anchor" href="#entity-integrity" aria-hidden="true">#</a> <strong>Entity Integrity</strong>:</h3><p>This ensures that there are no duplicate rows in a table. It&#39;s usually enforced by using a primary key.</p><h3 id="domain-integrity" tabindex="-1"><a class="header-anchor" href="#domain-integrity" aria-hidden="true">#</a> <strong>Domain Integrity</strong>:</h3><p>This enforces valid entries for a given column by restricting the type, the format, or the range of values.</p><h3 id="referential-integrity" tabindex="-1"><a class="header-anchor" href="#referential-integrity" aria-hidden="true">#</a> <strong>Referential Integrity</strong>:</h3><p>This ensures that the relationship between two tables remains consistent. It&#39;s typically enforced by using foreign keys.</p><h3 id="user-defined-integrity" tabindex="-1"><a class="header-anchor" href="#user-defined-integrity" aria-hidden="true">#</a> <strong>User-Defined Integrity</strong>:</h3><p>This enforces some specific business rules that do not fall into the other integrity categories. These are typically enforced by using triggers or stored procedures.</p><h2 id="what-is-a-query" tabindex="-1"><a class="header-anchor" href="#what-is-a-query" aria-hidden="true">#</a> What is a Query?</h2><p>A query in SQL is a request for data or information from a database. Queries can also be used to insert, update, or delete data, and to create, modify, or delete tables and other database objects.</p><h2 id="what-is-a-subquery-what-are-its-types" tabindex="-1"><a class="header-anchor" href="#what-is-a-subquery-what-are-its-types" aria-hidden="true">#</a> What is a Subquery? What are its types?</h2><p>A subquery in SQL is a query that is embedded within another query. It can be used to return data that will be used in the main query as a condition to further restrict the data to be retrieved. Subqueries can be used with SELECT, INSERT, UPDATE, and DELETE statements.</p><h3 id="correlated-subquery" tabindex="-1"><a class="header-anchor" href="#correlated-subquery" aria-hidden="true">#</a> <strong>Correlated Subquery</strong>:</h3><p>A correlated subquery works like a nested loop: the subquery depends on the outer query and executes once for every row worked on by the outer query. It uses values from the outer query in its WHERE clause.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> e1<span class="token punctuation">.</span>EmployeeName
<span class="token keyword">FROM</span> Employees e1
<span class="token keyword">WHERE</span> Salary <span class="token operator">&gt;</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> <span class="token function">AVG</span><span class="token punctuation">(</span>Salary<span class="token punctuation">)</span>
                <span class="token keyword">FROM</span> Employees e2
                <span class="token keyword">WHERE</span> e1<span class="token punctuation">.</span>DepartmentID <span class="token operator">=</span> e2<span class="token punctuation">.</span>DepartmentID<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="non-correlated-subquery" tabindex="-1"><a class="header-anchor" href="#non-correlated-subquery" aria-hidden="true">#</a> <strong>Non-Correlated Subquery</strong>:</h3><p>A non-correlated subquery executes first and passes its result to the outer query. It does not depend on the outer query.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> EmployeeName
<span class="token keyword">FROM</span> Employees
<span class="token keyword">WHERE</span> DepartmentID <span class="token operator">IN</span> <span class="token punctuation">(</span><span class="token keyword">SELECT</span> DepartmentID
                       <span class="token keyword">FROM</span> Departments
                       <span class="token keyword">WHERE</span> DeptName <span class="token operator">=</span> <span class="token string">&#39;Sales&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-are-some-common-clauses-used-with-select-query-in-sql" tabindex="-1"><a class="header-anchor" href="#what-are-some-common-clauses-used-with-select-query-in-sql" aria-hidden="true">#</a> What are some common clauses used with SELECT query in SQL?</h2><ul><li><strong>WHERE</strong> clause in SQL is used to filter records that are necessary, based on specific conditions.</li><li><strong>ORDER BY</strong> clause in SQL is used to sort the records based on some field(s) in ascending (<strong>ASC</strong>) or descending order (<strong>DESC</strong>).</li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span>
<span class="token keyword">FROM</span> myDB<span class="token punctuation">.</span>students
<span class="token keyword">WHERE</span> graduation_year <span class="token operator">=</span> <span class="token number">2019</span>
<span class="token keyword">ORDER</span> <span class="token keyword">BY</span> studentID <span class="token keyword">DESC</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><strong>GROUP BY</strong> clause in SQL is used to group records/rows that have the same values in specified columns into aggregated data. It&#39;s often used with aggregate functions (<code>COUNT</code>, <code>MAX</code>, <code>MIN</code>, <code>SUM</code>, <code>AVG</code>).</li><li><strong>HAVING</strong> clause in SQL is used to filter records in combination with the GROUP BY clause. It is different from WHERE, since the WHERE clause cannot filter aggregated records.</li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token function">COUNT</span><span class="token punctuation">(</span>studentId<span class="token punctuation">)</span><span class="token punctuation">,</span> country
<span class="token keyword">FROM</span> myDB<span class="token punctuation">.</span>students
<span class="token keyword">WHERE</span> country <span class="token operator">!=</span> <span class="token string">&quot;INDIA&quot;</span>
<span class="token keyword">GROUP</span> <span class="token keyword">BY</span> country
<span class="token keyword">HAVING</span> <span class="token function">COUNT</span><span class="token punctuation">(</span>studentID<span class="token punctuation">)</span> <span class="token operator">&gt;</span> <span class="token number">5</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-are-union-minus-and-intersect-commands" tabindex="-1"><a class="header-anchor" href="#what-are-union-minus-and-intersect-commands" aria-hidden="true">#</a> What are UNION, MINUS, and INTERSECT commands?</h2><h3 id="union" tabindex="-1"><a class="header-anchor" href="#union" aria-hidden="true">#</a> <strong>UNION</strong>:</h3><p>The <code>UNION</code> operator combines the result sets of two or more <code>SELECT</code> statements. It removes duplicate rows from the result. Each <code>SELECT</code> statement within the <code>UNION</code> must have the same number of columns with similar data types.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Students   <span class="token comment">/* Fetch the union of queries */</span>
<span class="token keyword">UNION</span>
<span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Contacts<span class="token punctuation">;</span>
<span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Students   <span class="token comment">/* Fetch the union of queries with duplicates*/</span>
<span class="token keyword">UNION</span> <span class="token keyword">ALL</span>
<span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Contacts<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="minus" tabindex="-1"><a class="header-anchor" href="#minus" aria-hidden="true">#</a> <strong>MINUS</strong>:</h3><p>The <code>MINUS</code> operator (known as <code>EXCEPT</code> in some databases) returns all rows in the first <code>SELECT</code> statement that are not returned by the second <code>SELECT</code> statement. The number and order of columns, as well as their types, must be the same in both <code>SELECT</code> statements.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Students   <span class="token comment">/* Fetch names from students */</span>
MINUS     <span class="token comment">/* that aren&#39;t present in contacts */</span>
<span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Contacts<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="intersect" tabindex="-1"><a class="header-anchor" href="#intersect" aria-hidden="true">#</a> <strong>INTERSECT</strong>:</h3><p>The <code>INTERSECT</code> operator returns all rows that are common to both <code>SELECT</code> statements. The number and order of columns, as well as their types, must be the same in both <code>SELECT</code> statements.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Students   <span class="token comment">/* Fetch names from students */</span>
<span class="token keyword">INTERSECT</span>    <span class="token comment">/* that are present in contacts as well */</span>
<span class="token keyword">SELECT</span> name <span class="token keyword">FROM</span> Contacts<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="datediff" tabindex="-1"><a class="header-anchor" href="#datediff" aria-hidden="true">#</a> datediff:</h2><p><code>DATEDIFF</code> is a function in SQL that is used to find the difference between two dates. It returns the difference in terms of the unit specified.</p><div class="language-mysql line-numbers-mode" data-ext="mysql"><pre class="language-mysql"><code>datediff(date1, date2)
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In this case, <code>DATEDIFF</code> returns the difference in days between <code>date1</code> and <code>date2</code>.</p><h2 id="what-are-entities-and-relationships" tabindex="-1"><a class="header-anchor" href="#what-are-entities-and-relationships" aria-hidden="true">#</a> What are Entities and Relationships?</h2><h3 id="entities" tabindex="-1"><a class="header-anchor" href="#entities" aria-hidden="true">#</a> <strong>Entities</strong>:</h3><p>An entity represents a real-world object that is being modeled in the database.</p><h3 id="relationships" tabindex="-1"><a class="header-anchor" href="#relationships" aria-hidden="true">#</a> <strong>Relationships</strong>:</h3><p>A relationship represents how entities are related to one another.</p><h2 id="list-the-different-types-of-relationships-in-sql" tabindex="-1"><a class="header-anchor" href="#list-the-different-types-of-relationships-in-sql" aria-hidden="true">#</a> List the different types of relationships in SQL</h2><ul><li><strong>One-to-One</strong>: Each row in table A is linked to no more than one row in table B. This is often used when you have two entities and there can be a direct and exclusive relationship between them. For example, a person and a national ID number.</li><li><strong>One-to-Many</strong>: Each row in table A can be related to many rows in table B, but each row in table B is related to only one row in table A. For example, a mother can have many children, but each child has only one mother.</li><li><strong>Many-to-Many</strong>: Rows in table A can be related to any number of rows in table B, and vice versa. This is often resolved into two one-to-many relationships with the use of a junction table. For example, students and classes. A student can enroll in many classes, and a class can include many students.</li></ul><h2 id="what-is-an-alias-in-sql" tabindex="-1"><a class="header-anchor" href="#what-is-an-alias-in-sql" aria-hidden="true">#</a> WHat is an Alias in SQL?</h2><p>An alias in SQL is a temporary name given to a table or a column in a table for the purpose of a particular SQL query. It&#39;s used for convenience and to make the query more readable.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> c<span class="token punctuation">.</span>CustomerName<span class="token punctuation">,</span> o<span class="token punctuation">.</span>OrderID
<span class="token keyword">FROM</span> Customers <span class="token keyword">AS</span> c
<span class="token keyword">JOIN</span> Orders <span class="token keyword">AS</span> o <span class="token keyword">ON</span> c<span class="token punctuation">.</span>CustomerID <span class="token operator">=</span> o<span class="token punctuation">.</span>CustomerID<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-is-a-view" tabindex="-1"><a class="header-anchor" href="#what-is-a-view" aria-hidden="true">#</a> What is a View?</h2><p>A view in SQL is a virtual table based on the result-set of an SQL statement. A view contains rows and columns, just like a real table. The fields in a view are fields from one or more real tables in the database.</p><h2 id="what-is-normalization" tabindex="-1"><a class="header-anchor" href="#what-is-normalization" aria-hidden="true">#</a> What is Normalization?</h2><p>Normalization in SQL is a process used to organize a database into tables and columns to minimize redundancy and dependency. The main aim of normalization is to add, delete, or modify data without causing data inconsistencies.</p><p>Normalization involves dividing a database into two or more tables and defining relationships between the tables. The main purpose of normalization is to reduce duplicate data, which not only reduces the amount of storage a database requires, but also improves data integrity.</p><p>There are several stages of normalization, each called a &quot;normal form.&quot; Here are the three most common normal forms:</p><ol><li><strong>First Normal Form</strong>: Each table cell should contain a single value, and each record needs to be unique.</li><li><strong>Second Normal Form</strong>: It includes all rules of 1NF and in addition to that, it mandates that all non-key attributes should be fully functionally dependent on the primary key.</li><li><strong>Third Normal Form</strong>: It includes all rules of 1NF and 2NF and in addition to that, it mandates that all columns should be directly dependent on the primary key. In other words, it should not have transitive dependency.</li></ol><h2 id="what-are-the-truncate-delete-and-drop-statements" tabindex="-1"><a class="header-anchor" href="#what-are-the-truncate-delete-and-drop-statements" aria-hidden="true">#</a> What are the TRUNCATE, DELETE, and DROP statements?</h2><ul><li><strong>DELETE</strong> statement is used to delete rows from a table.<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">DELETE</span> <span class="token keyword">FROM</span> Candidates
<span class="token keyword">WHERE</span> CandidateId <span class="token operator">&gt;</span> <span class="token number">1000</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div></li><li><strong>TRUNCATE</strong> command is used to delete all the rows from the table<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">TRUNCATE</span> <span class="token keyword">TABLE</span> Candidates<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><strong>DROP</strong> command is used to remove an object from the database.If we drop a table, all the rows in the table are deleted and the table structure is removed from the database.<div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">DROP</span> <span class="token keyword">TABLE</span> Candidates<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ul><h2 id="what-are-aggregate-and-scalar-functions" tabindex="-1"><a class="header-anchor" href="#what-are-aggregate-and-scalar-functions" aria-hidden="true">#</a> What are Aggregate and Scalar functions?</h2><ul><li><p><strong>Aggregate Functions</strong>: Aggregate functions operate on a set of values but return a single summarizing value. They are often used with the <code>GROUP BY</code> clause in a <code>SELECT</code> statement. Examples of aggregate functions include:</p><ul><li><code>COUNT()</code>: returns the number of rows</li><li><code>SUM()</code>: returns the sum of the values</li><li><code>AVG()</code>: returns the average of the values</li><li><code>MIN()</code>: returns the smallest value</li><li><code>MAX()</code>: returns the largest value</li></ul></li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token function">COUNT</span><span class="token punctuation">(</span>CustomerID<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> Customers<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><strong>Scalar Functions</strong>: Scalar functions operate on a single value and then return a single value. Scalar functions can be used wherever an expression is valid. Examples of scalar functions include: <ul><li><code>UCASE()</code>: converts a field to upper case</li><li><code>LCASE()</code>: converts a field to lower case</li><li><code>ROUND()</code>: rounds a numeric field to the number of decimals specified</li><li><code>LEN()</code>: returns the length of a text field</li><li><code>NOW()</code>: returns the current date &amp; time</li></ul></li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token function">UCASE</span><span class="token punctuation">(</span>FirstName<span class="token punctuation">)</span>
<span class="token keyword">FROM</span> Customers<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="how-to-create-empty-tables-with-the-same-structure-as-another-table" tabindex="-1"><a class="header-anchor" href="#how-to-create-empty-tables-with-the-same-structure-as-another-table" aria-hidden="true">#</a> How to create empty tables with the same structure as another table?</h2><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">INTO</span> Students_copy
<span class="token keyword">FROM</span> Students <span class="token keyword">WHERE</span> <span class="token number">1</span> <span class="token operator">=</span> <span class="token number">2</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="what-is-pattern-matching-in-sql" tabindex="-1"><a class="header-anchor" href="#what-is-pattern-matching-in-sql" aria-hidden="true">#</a> What is pattern matching in SQL?</h2><p>Pattern matching in SQL is a technique used to find rows that match a specific pattern. It&#39;s often used in the <code>WHERE</code> clause of a <code>SELECT</code>, <code>UPDATE</code>, or <code>DELETE</code> statement.</p><p>The most common way to do pattern matching in SQL is using the <code>LIKE</code> operator, which allow us to use two special characters:</p><ul><li><code>%</code>: Represents zero, one, or multiple characters.</li><li><code>_</code>: Represents a single character</li></ul><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">SELECT</span> <span class="token operator">*</span> <span class="token keyword">FROM</span> Customers
<span class="token keyword">WHERE</span> CustomerName <span class="token operator">LIKE</span> <span class="token string">&#39;a%&#39;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>This SQL statement selects all customers whose names start with &quot;a&quot;</p><h2 id="insert-in-sql" tabindex="-1"><a class="header-anchor" href="#insert-in-sql" aria-hidden="true">#</a> INSERT in SQL:</h2><h3 id="inserting-complete-rows" tabindex="-1"><a class="header-anchor" href="#inserting-complete-rows" aria-hidden="true">#</a> <strong>Inserting complete rows</strong>:</h3><p>We can specify values for all columns in the table.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">INSERT</span> <span class="token keyword">INTO</span> table_name <span class="token punctuation">(</span>column1<span class="token punctuation">,</span> column2<span class="token punctuation">,</span> column3<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span>
<span class="token keyword">VALUES</span> <span class="token punctuation">(</span>value1<span class="token punctuation">,</span> value2<span class="token punctuation">,</span> value3<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="inserting-specific-columns" tabindex="-1"><a class="header-anchor" href="#inserting-specific-columns" aria-hidden="true">#</a> <strong>Inserting specific columns</strong>:</h3><p>We can specify values for only certain columns, and the other columns will be filled with their default values.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">INSERT</span> <span class="token keyword">INTO</span> table_name <span class="token punctuation">(</span>column1<span class="token punctuation">,</span> column2<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span>
<span class="token keyword">VALUES</span> <span class="token punctuation">(</span>value1<span class="token punctuation">,</span> value2<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="update-in-sql" tabindex="-1"><a class="header-anchor" href="#update-in-sql" aria-hidden="true">#</a> UPDATE in SQL</h2><p>The <code>UPDATE</code> statement in SQL is used to modify existing records in a table. You can change the data in a single column or in multiple columns at once.</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">UPDATE</span> table_name
<span class="token keyword">SET</span> column1 <span class="token operator">=</span> value1<span class="token punctuation">,</span> column2 <span class="token operator">=</span> value2<span class="token punctuation">,</span> <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token keyword">WHERE</span> condition<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="example" tabindex="-1"><a class="header-anchor" href="#example" aria-hidden="true">#</a> example:</h3><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">UPDATE</span> Customers
<span class="token keyword">SET</span> Email <span class="token operator">=</span> <span class="token string">&#39;newemail@example.com&#39;</span>
<span class="token keyword">WHERE</span> CustomerID <span class="token operator">=</span> <span class="token number">1</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="create-in-sql" tabindex="-1"><a class="header-anchor" href="#create-in-sql" aria-hidden="true">#</a> CREATE in SQL:</h2><p>The <code>CREATE</code> statement is used to create a new database object. Here&#39;s how you can create a new table:</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> table_name <span class="token punctuation">(</span>
    column1 datatype <span class="token keyword">constraint</span><span class="token punctuation">,</span>
    column2 datatype <span class="token keyword">constraint</span><span class="token punctuation">,</span>
    <span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="example-3" tabindex="-1"><a class="header-anchor" href="#example-3" aria-hidden="true">#</a> example:</h3><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">CREATE</span> <span class="token keyword">TABLE</span> Customers <span class="token punctuation">(</span>
    CustomerID <span class="token keyword">int</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
    CustomerName <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span> <span class="token operator">NOT</span> <span class="token boolean">NULL</span><span class="token punctuation">,</span>
    ContactName <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span>
<span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="alter-in-sql" tabindex="-1"><a class="header-anchor" href="#alter-in-sql" aria-hidden="true">#</a> ALTER in SQL:</h2><p>The <code>ALTER</code> statement is used to add, modify, or delete columns in an existing table, or to change the data type of a column in a table. Here&#39;s how you can add a column to an existing table:</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> table_name
<span class="token keyword">ADD</span> column_name datatype <span class="token keyword">constraint</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="example-4" tabindex="-1"><a class="header-anchor" href="#example-4" aria-hidden="true">#</a> example:</h3><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token keyword">ALTER</span> <span class="token keyword">TABLE</span> Customers
<span class="token keyword">ADD</span> Email <span class="token keyword">varchar</span><span class="token punctuation">(</span><span class="token number">255</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,170),c=[d];function p(u,h){return a(),n("div",null,c)}const k=e(l,[["render",p],["__file","sql.html.vue"]]);export{k as default};
