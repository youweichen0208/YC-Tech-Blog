import{_ as a,o as n,c as s,e}from"./app-vcqKu50p.js";const t="/assets/images/spring-jdbc.png",i={},o=e('<h1 id="jpa-and-hibernate" tabindex="-1"><a class="header-anchor" href="#jpa-and-hibernate" aria-hidden="true">#</a> JPA and Hibernate</h1><h2 id="what-is-jdbc" tabindex="-1"><a class="header-anchor" href="#what-is-jdbc" aria-hidden="true">#</a> What is JDBC</h2><p>JDBC stands for Java Database Connectivity. It is a Java-based API that allows Java applications to interact with relational databases. JDBC provides a standard interface for connecting to databases, executing SQL queries, and processing the results</p><h2 id="jdbc-to-spring-jdbc" tabindex="-1"><a class="header-anchor" href="#jdbc-to-spring-jdbc" aria-hidden="true">#</a> JDBC to Spring JDBC</h2><figure><img src="'+t+`" alt="JDBC to Spring JDBC" tabindex="0" loading="lazy"><figcaption>JDBC to Spring JDBC</figcaption></figure><ul><li>JDBC: <ul><li>Write a lot of SQL queries!</li><li>And write a lot of Java code</li></ul></li><li>Spring JDBC: <ul><li>Write a lot of SQL queries</li><li>BUT lesser Java code</li></ul></li></ul><h3 id="schema-sql-in-springboot" tabindex="-1"><a class="header-anchor" href="#schema-sql-in-springboot" aria-hidden="true">#</a> schema.sql in SpringBoot</h3><p>In Spring Boot, the <code>schema.sql</code> file is used for defining the database schema, and it plays a role in the automatic initialization of the database.</p><ol><li>Automatic Database Initialization:</li></ol><ul><li>When Spring Boot detects the presence of a file named <code>schema.sql</code> in the classpath (usually in the <code>src/main/resources</code> directory), it automatically executes the SQL statements from the file during the application startup.</li></ul><ol start="2"><li>Defining Database Schema:</li></ol><ul><li>The <code>schema.sql</code> file contains SQL statements for creating tables, indexes, and other database objects. It serves as a blueprint for the initial structure of the database.</li></ul><ol start="3"><li>Customization with <code>data.sql</code>:</li></ol><ul><li>In Spring Boot, the <code>data.sql</code> file is used for automatically populating the database with initial data during the application setup (like insert statements).</li><li>Apart from <code>schema.sql</code>, Spring Boot also recognizes a file named <code>data.sql</code> for initializing data. If both <code>schema.sql</code> and <code>data.sql</code> are present, the schema is created first, followed by the insertion of data.</li></ul><h2 id="h2-in-spring" tabindex="-1"><a class="header-anchor" href="#h2-in-spring" aria-hidden="true">#</a> H2 in Spring</h2><p>In Spring Boot, H2 is a popular in-memory database that is often used for development, testing, and prototyping. An in-memory database is a type of database system that primarily relies on main memory (RAM) for temporarily storing and managing data.</p><h3 id="add-h2-dependency" tabindex="-1"><a class="header-anchor" href="#add-h2-dependency" aria-hidden="true">#</a> Add H2 Dependency</h3><div class="language-xml line-numbers-mode" data-ext="xml"><pre class="language-xml"><code>	<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>dependency</span><span class="token punctuation">&gt;</span></span>
			<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>groupId</span><span class="token punctuation">&gt;</span></span>com.h2database<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>groupId</span><span class="token punctuation">&gt;</span></span>
			<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>artifactId</span><span class="token punctuation">&gt;</span></span>h2<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>artifactId</span><span class="token punctuation">&gt;</span></span>
			<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>scope</span><span class="token punctuation">&gt;</span></span>runtime<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>scope</span><span class="token punctuation">&gt;</span></span>
	<span class="token tag"><span class="token tag"><span class="token punctuation">&lt;/</span>dependency</span><span class="token punctuation">&gt;</span></span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In this case, <code>&lt;scope&gt;runtime&lt;/scope&gt;</code> means that the H2 dependency is only needed during the runtime phase of our application and is not required for compilation. The H2 library is used by the application when it is running, but it is not necessary during the compilation and build process.</p><h3 id="configure-the-properties-in-applications-properties" tabindex="-1"><a class="header-anchor" href="#configure-the-properties-in-applications-properties" aria-hidden="true">#</a> Configure the properties in applications.properties</h3><div class="language-properties line-numbers-mode" data-ext="properties"><pre class="language-properties"><code><span class="token key attr-name">spring.datasource.url</span><span class="token punctuation">=</span><span class="token value attr-value">jdbc:h2:mem:testdb</span>
<span class="token key attr-name">spring.datasource.driverClassName</span><span class="token punctuation">=</span><span class="token value attr-value">org.h2.Driver</span>
<span class="token key attr-name">spring.datasource.username</span><span class="token punctuation">=</span><span class="token value attr-value">sa</span>
<span class="token key attr-name">spring.datasource.password</span><span class="token punctuation">=</span><span class="token value attr-value">password</span>
<span class="token key attr-name">spring.h2.console.enabled</span><span class="token punctuation">=</span><span class="token value attr-value">true</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol><li><code>spring.datasource.url=jdbc:h2:mem:testdb</code></li></ol><ul><li>This property sets the JDBC URL for the H2 in-memory database. The URL <code>jdbc:h2:mem:testdb</code> indicates that an in-memory database named &quot;testdb&quot; will be used. This type of database is created in memory and doesn&#39;t persist data beyond the application&#39;s lifecycle.</li></ul><ol start="2"><li><code>spring.datasource.driverClassName=org.h2.Driver</code>:</li></ol><ul><li>Specifies the fully qualified name of the JDBC driver class to use for the H2 database. In this case, it is set to the H2 database driver class, <code>org.h2.Driver</code>.</li></ul><ol start="3"><li><code>spring.datasource.username=sa</code>:</li></ol><ul><li>Sets the username for connecting to the H2 database. In this example, the username is set to &quot;sa&quot;.</li></ul><ol start="4"><li><code>spring.datasource.password=password</code>:</li></ol><ul><li>Sets the password for connecting to the H2 database. In this example, the password is set to &quot;password&quot;</li></ul><ol start="5"><li><code>spring.h2.console.enabled=true</code>:</li></ol><ul><li>This property enables the H2 console, which is a web-based console provided by H2 for interacting with the in-memory database. If set to <code>true</code>, we can access the H2 console at <code>http://localhost:8080/h2-console</code></li></ul><h2 id="repository" tabindex="-1"><a class="header-anchor" href="#repository" aria-hidden="true">#</a> @Repository</h2><p>In Spring Framework, the <code>@Repository</code> annotation is part of the stereotype annotations and is used to indicate that a class is a Data Access Object(DAO). The primary purpose of the <code>@Repository</code> annotation is to specify that the annotated class is responsible for encapsulating the interaction with a data source, typically a database.</p><ol><li>DAO Design Pattern:</li></ol><ul><li>The DAO pattern separates the data access code from the business logic, providing a clean and modular way to interact with the data source.</li></ul><ol start="2"><li>Component Scanning:</li></ol><ul><li>When component scanning is enabled in a Spring application, classes annotated with <code>@Repository</code> are automatically discovered and registered as Spring beans.</li></ul><ol start="3"><li>Automatic Bean Definition:</li></ol><ul><li>Classes annotated with <code>@Repository</code> are automatically considered as Spring beans, and Spring manages their lifecyle. This means that we can inject instances of these classes into other components, such as services or controllers.</li></ul><h2 id="jdbctemplate" tabindex="-1"><a class="header-anchor" href="#jdbctemplate" aria-hidden="true">#</a> JdbcTemplate</h2><p>In Spring Framework, <code>JdbcTemplate</code> is a class that simplifies database access and interaction with relational databases using JDBC. It is part of the JDBC support provided by the Spring Framework and is used to streamline common database operations without the need for boilerplate code. JDBC serves as the standard Java API for connecting and interacting with relational databases. It provides a set of interfaces and classes that allow Java applications to communicate with databases by executing SQL queries, updates and other database operations. JDBC acts as the medium or bridge between Java classes and relational databases.</p><h2 id="spring-jdbc" tabindex="-1"><a class="header-anchor" href="#spring-jdbc" aria-hidden="true">#</a> Spring JDBC</h2><p>Spring JDBC facilitates database access in Java applications by handling many of the low-level details, reducing the amount of boilerplate code required, and offering features to enhance productivity and maintainability.</p><h3 id="_1-autowire-the-jdbctemplate" tabindex="-1"><a class="header-anchor" href="#_1-autowire-the-jdbctemplate" aria-hidden="true">#</a> 1. autowire the JdbcTemplate</h3><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token annotation punctuation">@Autowired</span>
<span class="token keyword">private</span> <span class="token class-name">JdbcTemplate</span> springJdbcTemplate<span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Use dependency injection and autowire the <code>JdbcTemplate</code> bean into the classes that need to interact with the database. Autowiring simplifies the process of obtaining a <code>JdbcTemplate</code> instance and promotes a cleaner and more modular design.</p><h3 id="_2-write-the-sql" tabindex="-1"><a class="header-anchor" href="#_2-write-the-sql" aria-hidden="true">#</a> 2. write the SQL</h3><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code>    <span class="token keyword">private</span> <span class="token keyword">static</span> <span class="token class-name">String</span> <span class="token constant">INSERT_QUERY</span> <span class="token operator">=</span>
            <span class="token triple-quoted-string string">&quot;&quot;&quot;
                insert into course (id, name, author) values (?, ?, ?);
            &quot;&quot;&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li>we are using the triple double-quotes(<code>&quot;&quot;&quot;</code>) to define a multi-line string literal in Java.</li><li>The placeholders(<code>?, ?, ?</code>) are typically used for parameterized queries where we later provide actual values during execution.</li></ul><h3 id="_3-execute-an-sql" tabindex="-1"><a class="header-anchor" href="#_3-execute-an-sql" aria-hidden="true">#</a> 3. execute an SQL</h3><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">insert</span><span class="token punctuation">(</span><span class="token class-name">Course</span> course<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    springJdbcTemplate<span class="token punctuation">.</span><span class="token function">update</span><span class="token punctuation">(</span><span class="token constant">INSERT_QUERY</span><span class="token punctuation">,</span> course<span class="token punctuation">.</span><span class="token function">getId</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> course<span class="token punctuation">.</span><span class="token function">getName</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">,</span> course<span class="token punctuation">.</span><span class="token function">getAuthor</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>update</code> method: The <code>update</code> method of <code>JdbcTemplate</code> is used for executing SQL statements that modify data in the database (e.g. insert, update, delete). It takes the SQL statement and an array of parameters as arguments.</li></ul><h2 id="rowmapper-in-spring-jdbc" tabindex="-1"><a class="header-anchor" href="#rowmapper-in-spring-jdbc" aria-hidden="true">#</a> RowMapper in Spring JDBC</h2><p>A <code>RowMapper</code> is an interface used to map a row of <code>ResultSet</code> to an object. It is commonly used in JDBC operations where the result of a query needs to be converted into Java objects. The <code>RowMapper</code> interface provides a mechanism to customize how the data from the result set is mapped to the corresponding Java object.</p><p>Example:</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token class-name">Course</span> <span class="token function">findById</span><span class="token punctuation">(</span><span class="token keyword">int</span> id<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">// Resultset -&gt; Bean =&gt; Row Mapper =&gt;</span>
    <span class="token keyword">return</span> springJdbcTemplate<span class="token punctuation">.</span><span class="token function">queryForObject</span><span class="token punctuation">(</span><span class="token constant">SELECT_QUERY</span><span class="token punctuation">,</span> <span class="token keyword">new</span> <span class="token class-name">BeanPropertyRowMapper</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token punctuation">&gt;</span></span><span class="token punctuation">(</span><span class="token class-name">Course</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">)</span><span class="token punctuation">,</span> id<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>

</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ul><li><code>queryForObject</code> method: this method is used to execute a <code>SELECT</code> query and retrieve a single result, mapping it to a Java object.</li><li><code>new BeanPropertyRowMapper&lt;&gt;(Course.class)</code>: this is an instance of <code>BeanPropertyRowMapper</code>, a built-in implementation of <code>RowMapper</code> provided by Spring JDBC. It maps the result set to a Java object using JavaBeans conventions, assuming that the property names in the <code>Course</code> class match the column names in the result set.</li><li>The <code>queryForObject</code> method takes care of executing the SQL query, binding parameters, and converting the result set to a Java object. If the query returns no results or more than one result, it will throw a <code>IncorrectResultSizeDataAccessException</code>, so we need to ensure that our query always returns exactly one result for the specified ID.</li></ul><h2 id="commandlinerunner" tabindex="-1"><a class="header-anchor" href="#commandlinerunner" aria-hidden="true">#</a> CommandLineRunner</h2><p>In Spring Framework, <code>CommandLineRunner</code> is an interface that can be implemented by Spring beans to indicate that they should run when the application is started.</p><h3 id="run" tabindex="-1"><a class="header-anchor" href="#run" aria-hidden="true">#</a> run</h3><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">import</span> <span class="token import"><span class="token namespace">org<span class="token punctuation">.</span>springframework<span class="token punctuation">.</span>boot<span class="token punctuation">.</span></span><span class="token class-name">CommandLineRunner</span></span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token import"><span class="token namespace">org<span class="token punctuation">.</span>springframework<span class="token punctuation">.</span>stereotype<span class="token punctuation">.</span></span><span class="token class-name">Component</span></span><span class="token punctuation">;</span>

<span class="token annotation punctuation">@Component</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">MyCommandLineRunner</span> <span class="token keyword">implements</span> <span class="token class-name">CommandLineRunner</span> <span class="token punctuation">{</span>

    <span class="token annotation punctuation">@Override</span>
    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">run</span><span class="token punctuation">(</span><span class="token class-name">String</span><span class="token punctuation">.</span><span class="token punctuation">.</span><span class="token punctuation">.</span> args<span class="token punctuation">)</span> <span class="token keyword">throws</span> <span class="token class-name">Exception</span> <span class="token punctuation">{</span>
        <span class="token comment">// Your custom initialization logic goes here</span>
        <span class="token class-name">System</span><span class="token punctuation">.</span>out<span class="token punctuation">.</span><span class="token function">println</span><span class="token punctuation">(</span><span class="token string">&quot;Application started. Performing initialization tasks...&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>run</code> method is called by the Spring Boot application after the application context has been loaded and just before the application starts running. This is particularly useful for tasks that need to be executed at application startup, such as database initialization, data loading, or any other custom initialization steps.</p><h2 id="what-is-jpa" tabindex="-1"><a class="header-anchor" href="#what-is-jpa" aria-hidden="true">#</a> What is JPA?</h2><p>JPA stands for Java Persistence API. It is a Java specification for managing, persisting, and accessing data between Java objects and a relational database. Persistence refers to the process of saving a new entity object to the database.</p><ol><li>Entity Classes:</li></ol><ul><li>JPA introduces the concept of entity classes, which are regular Java classes representing objects that can be stored in a database. Each entity class is associated with a table in the database.</li></ul><ol start="2"><li>Object-Relational Mapping(ORM):</li></ol><ul><li>JPA enables developers to map Java objects to database tables and vice versa. This mapping is done through annotations or XML configurations, and it allows for transparent and bidirectional data and bidirectional data exchange between Java objects and the database.</li></ul><ol start="3"><li>Annotations:</li></ol><ul><li>JPA uses annotations to define the mapping between entity classes and database tables. Annotations like <code>@Entity</code>, <code>@Table</code>, <code>@Id</code>, and others are used to specify the structure and behavior of entities.</li></ul><ol start="4"><li>EntityManager:</li></ol><ul><li>The <code>EntityManager</code> is a key interface in JPA that provides methods for performing CRUD operations on entities.</li></ul><h3 id="entity-column-id" tabindex="-1"><a class="header-anchor" href="#entity-column-id" aria-hidden="true">#</a> @Entity, @Column, @Id</h3><ul><li>@Entity: <ul><li>The <code>@Entity</code> annotation is used to mark a Java class as a JPA entity. An entity represents a table in a relational database. Each instance of the entity class corresponds to a row in the database table. The <code>@Entity</code> annotation is typically placed on the class level.</li></ul></li><li>@Id: <ul><li>The <code>@Id</code> annotation designates a field or property as the primary key of the entity. It indicates that the corresponding attribute uniquely identifies each record in the database table.</li></ul></li><li>@Column: <ul><li>The <code>@Column</code> annotation is sued to customize the mapping of an entity attribute to a database table column. It allows us to specify various properties such as column name, length, etc.</li></ul></li></ul><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token annotation punctuation">@Entity</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">Course</span> <span class="token punctuation">{</span>
    <span class="token annotation punctuation">@Id</span>
    <span class="token keyword">private</span> <span class="token keyword">long</span> id<span class="token punctuation">;</span>

    <span class="token annotation punctuation">@Column</span><span class="token punctuation">(</span>name<span class="token operator">=</span><span class="token string">&quot;name&quot;</span><span class="token punctuation">)</span>
    <span class="token keyword">private</span> <span class="token class-name">String</span> name<span class="token punctuation">;</span>

    <span class="token annotation punctuation">@Column</span><span class="token punctuation">(</span>name<span class="token operator">=</span><span class="token string">&quot;author&quot;</span><span class="token punctuation">)</span>
    <span class="token keyword">private</span> <span class="token class-name">String</span> author<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="transactional" tabindex="-1"><a class="header-anchor" href="#transactional" aria-hidden="true">#</a> @Transactional</h3><p><code>@Transactional</code> helps manage transactions effectively when interacting with a relational database.</p><ol><li>Atomicity of Operations:</li></ol><ul><li>Transactions in JPA ensure that a sequence of database operations either all succeed or all fail as a single, atomic unit.</li></ul><ol start="2"><li>Consistency and Data Integrity:</li></ol><ul><li>Transactions help maintain data consistency and integrity by ensuring that the database transitions from one consistent state to another. Without transactions, multiple database operations might lead to inconsistencies if some operations succeed while others fail.</li></ul><ol start="3"><li>Synchronization with EntityManager:</li></ol><ul><li>JPA relies on the <code>EntityManager</code> to manage the lifecycle of persistent objects. The <code>@Transactional</code> annotation ensures that the <code>EntityManager</code> is aware of the transactional context, and it manages the synchronization of changes made to entities with the underlying database.</li></ul><h3 id="persistencecontext-entitymanager" tabindex="-1"><a class="header-anchor" href="#persistencecontext-entitymanager" aria-hidden="true">#</a> @PersistenceContext, EntityManager</h3><p>In JPA, the <code>@PersistenceContext</code> annotation is used to inject an <code>EntityManager</code> into a Spring-managed bean. The <code>EntityManager</code> is a key interface in JPA that provides methods for interacting with the underlying database. The <code>@PersistenceContext</code> annotation is typically used in Spring components, such as service classes or repositories, to obtain and manage the <code>EntityManager</code> for performing CRUD operations.</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token annotation punctuation">@Repository</span>
<span class="token annotation punctuation">@Transactional</span>
<span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">CourseJpaRepository</span> <span class="token punctuation">{</span>

    <span class="token annotation punctuation">@PersistenceContext</span>
    <span class="token keyword">private</span> <span class="token class-name">EntityManager</span> entityManager<span class="token punctuation">;</span>

    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">insert</span><span class="token punctuation">(</span><span class="token class-name">Course</span> course<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        entityManager<span class="token punctuation">.</span><span class="token function">merge</span><span class="token punctuation">(</span>course<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">public</span> <span class="token class-name">Course</span> <span class="token function">findById</span><span class="token punctuation">(</span><span class="token keyword">long</span> id<span class="token punctuation">)</span> <span class="token punctuation">{</span>
       <span class="token keyword">return</span> entityManager<span class="token punctuation">.</span><span class="token function">find</span><span class="token punctuation">(</span><span class="token class-name">Course</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> id<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">public</span> <span class="token keyword">void</span> <span class="token function">delete</span><span class="token punctuation">(</span><span class="token keyword">long</span> id<span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token class-name">Course</span> course <span class="token operator">=</span> entityManager<span class="token punctuation">.</span><span class="token function">find</span><span class="token punctuation">(</span><span class="token class-name">Course</span><span class="token punctuation">.</span><span class="token keyword">class</span><span class="token punctuation">,</span> id<span class="token punctuation">)</span><span class="token punctuation">;</span>
        entityManager<span class="token punctuation">.</span><span class="token function">remove</span><span class="token punctuation">(</span>course<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="spring-data-jpa" tabindex="-1"><a class="header-anchor" href="#spring-data-jpa" aria-hidden="true">#</a> Spring Data JPA</h3><p>Spring Data JPA allows you to use the full power of Spring and JPA together, with only a minimal amount of coding. It provides a way to implement JPA repositories without needing to write a lot of code or SQL.</p><ol><li>Repository Interfaces:</li></ol><ul><li>Spring Data JPA introduces the concept of repository interfaces. A repository interface is an interface that defines a set of methods for performing CURD operations on entities. These interfaces are not implemented by developers; Instead, Spring Data JPA dynamically generates the implementation at runtime based on method names and conventions.</li></ul><ol start="2"><li>Entity Management:</li></ol><ul><li>Spring Data JPA simplifies entity management by automatically providing implementations for common database operations. Developers can create repository interfaces for their entities and use them to interact with the underlying database without writing boilerplace code for basic CRUD operations.</li></ul><ol start="3"><li>Method Query derivation:</li></ol><ul><li>The ability to generate queries dynamically simplifies the development process by reducing the need for developers to write explicit JPQL.</li><li>The method name is parsed to identify specific keywords that convey the intended operation. For example, keywords like <code>findBy</code>, <code>deleteBy</code>, or <code>countBy</code> signal the type of operation(selecting, deleting, or counting records)</li></ul><ol start="4"><li>Pagination and Sorting:</li></ol><ul><li>Spring Data JPA supports built-in pagination and sorting for query results. Developers can include <code>Pageable</code> and <code>Sort</code> parameters in repository methods to retrieve paginated and sorted lists of entities.</li></ul><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">interface</span> <span class="token class-name">CourseSpringDataJpaRepository</span> <span class="token keyword">extends</span> <span class="token class-name">JpaRepository</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Course</span><span class="token punctuation">,</span> <span class="token class-name">Long</span><span class="token punctuation">&gt;</span></span> <span class="token punctuation">{</span>

    <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Course</span><span class="token punctuation">&gt;</span></span> <span class="token function">findByAuthor</span><span class="token punctuation">(</span><span class="token class-name">String</span> author<span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token class-name">List</span><span class="token generics"><span class="token punctuation">&lt;</span><span class="token class-name">Course</span><span class="token punctuation">&gt;</span></span> <span class="token function">findByName</span><span class="token punctuation">(</span><span class="token class-name">String</span> name<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="hibernate-vs-jpa" tabindex="-1"><a class="header-anchor" href="#hibernate-vs-jpa" aria-hidden="true">#</a> Hibernate vs JPA</h2><ul><li><p>JPA defines the specification. It is an API</p><ul><li>how do we define entities?</li><li>how do we map attributes?</li><li>who manages the entities?</li></ul></li><li><p>Hibernate is one of the popular implementations of JPA.</p></li></ul>`,99),c=[o];function l(p,r){return n(),s("div",null,c)}const u=a(i,[["render",l],["__file","hibernate.html.vue"]]);export{u as default};
