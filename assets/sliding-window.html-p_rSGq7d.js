import{_ as n,o as e,c as a,e as s}from"./app-pi6YeQEA.js";const t={},i=s(`<h1 id="sliding-window" tabindex="-1"><a class="header-anchor" href="#sliding-window" aria-hidden="true">#</a> Sliding Window</h1><h3 id="_1-when-should-we-use-sliding-window" tabindex="-1"><a class="header-anchor" href="#_1-when-should-we-use-sliding-window" aria-hidden="true">#</a> 1. When should we use sliding window?</h3><ol><li>the problem will either explicitly or implicitly define criteria that make a subarray &quot;valid&quot;: <ol><li>constraint metric: asking for attributes of a subarray. It could be the sum, the number of unique elements, the frequency of a specific element, or any other attribute.</li><li>a numeric restriction on the constraint metric. This is what the constraint metric should be for a subarray to be considered valid.</li></ol></li><li>the problem will ask you to find valid subarrays in some way. <ol><li>the most common task you will see is finding the best valid subarray. The problem will define what makes a subarray better than another. For example, a problem might ask you to find the longest valid subarray.</li><li>another common task is finding the number of valid subarrays.</li></ol></li></ol><h3 id="_2-pseudocode-for-a-general-template" tabindex="-1"><a class="header-anchor" href="#_2-pseudocode-for-a-general-template" aria-hidden="true">#</a> 2. pseudocode for a general template:</h3><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">public</span> <span class="token keyword">int</span> <span class="token function">fn</span><span class="token punctuation">(</span><span class="token keyword">int</span><span class="token punctuation">[</span><span class="token punctuation">]</span> arr<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">int</span> left <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> ans <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">,</span> curr <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span>

    <span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">int</span> right <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> right <span class="token operator">&lt;</span> arr<span class="token punctuation">.</span>length<span class="token punctuation">;</span> right<span class="token operator">++</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        <span class="token comment">// do logic here to add arr[right] to curr</span>

        <span class="token keyword">while</span> <span class="token punctuation">(</span><span class="token constant">WINDOW_CONDITION_BROKEN</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
            <span class="token comment">// remove arr[left] from curr</span>
            left<span class="token operator">++</span><span class="token punctuation">;</span>
        <span class="token punctuation">}</span>

        <span class="token comment">// update ans</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">return</span> ans<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><ol><li><code>Initialization:</code> We start by initializing two pointers, <code>left</code> and <code>right</code>, to the start of the array. We also initialize a variable <code>curr</code> to store the current state of the window, and <code>ans</code> to store the final answer.</li><li><code>Expand the Window:</code> we start expanding the window by moving <code>right</code> pointer towards the end of the array. As we add new elements into the window, we update the <code>curr</code> state accordingly.</li><li><code>Window Condition Check:</code> After each expansion, we check if the window condition is broken. This condition is problem-specific and can be anything like the sum of the window exceeding a certain value, the window containing a certain number of distinct elements, etc.</li><li><code>Shrink the Window:</code> If the window condition is broken, we start shrinking the window from the left by moving the <code>left</code> pointer towards the right. As we remove elements from the window, we update the <code>curr</code> state accordingly. We keep shrinking the window until the window condition is satisfied again.</li><li><code>Update the Answer:</code> After each expansion, we update our final answer <code>ans</code> based on the current window state <code>curr</code>. This could be the maximum or minimum window size, the maximum or minimum window sum, etc.</li><li><code>Repeat:</code> We repeat steps 2-5 until the <code>right</code> pointer has gone through every element in the array.</li></ol><h3 id="_3-time-complexity" tabindex="-1"><a class="header-anchor" href="#_3-time-complexity" aria-hidden="true">#</a> 3. Time Complexity:</h3><p>The time complexity of the sliding window algorithm is generally O(n)</p><h3 id="_4-related-techniques-we-can-use-in-sliding-window" tabindex="-1"><a class="header-anchor" href="#_4-related-techniques-we-can-use-in-sliding-window" aria-hidden="true">#</a> 4. Related techniques we can use in sliding window:</h3><ol><li>left &amp; right pointer</li><li>Use integer array to represent 26 English alphaset based on the ASCII value and keep track of frequency count of characters (usually used to handle the frequency problems related to 26 characters and deal with longest/maximum problems).</li></ol><p>For example,</p><div class="language-java line-numbers-mode" data-ext="java"><pre class="language-java"><code><span class="token keyword">int</span><span class="token punctuation">[</span><span class="token punctuation">]</span> count <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token keyword">int</span><span class="token punctuation">[</span><span class="token number">26</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token comment">//&#39;B&#39; - &#39;A&#39; = 1, &#39;C&#39; - &#39;A&#39; = 2, and so forth...</span>
count<span class="token punctuation">[</span><span class="token char">&#39;B&#39;</span> <span class="token operator">-</span> <span class="token char">&#39;A&#39;</span><span class="token punctuation">]</span><span class="token operator">++</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,12),o=[i];function l(c,r){return e(),a("div",null,o)}const p=n(t,[["render",l],["__file","sliding-window.html.vue"]]);export{p as default};
