import{_ as n,o as a,c as s,e}from"./app-tCtUkKLP.js";const t="/YC-Tech-Blog/assets/images/nextjs-img1.png",i="/YC-Tech-Blog/assets/images/nextjs-img2.png",o={},c=e('<h1 id="nextjs" tabindex="-1"><a class="header-anchor" href="#nextjs" aria-hidden="true">#</a> NextJs</h1><h2 id="what-is-nextjs" tabindex="-1"><a class="header-anchor" href="#what-is-nextjs" aria-hidden="true">#</a> What is NextJS?</h2><p>Next.JS is a popular open-source React framework for building server-side rendered (SSR) and static web applications.</p><h2 id="advantages-of-next-js" tabindex="-1"><a class="header-anchor" href="#advantages-of-next-js" aria-hidden="true">#</a> Advantages of Next.js</h2><ol><li>Server-side rendering (SSR): Next.js allows us to render our React components on the server, which can improve the initial load time of our application and provide better SEO (Search Engine Optimization) performance.</li><li>Static Site Generation (SSG): Next.js supports generating static HTML files at build time, which can be served dorectly by a CDN, resulting in faster load times and better scalability.</li></ol><h2 id="server-side-rendering-vs-traditional-client-side-rendering" tabindex="-1"><a class="header-anchor" href="#server-side-rendering-vs-traditional-client-side-rendering" aria-hidden="true">#</a> Server-Side Rendering vs traditional client-side rendering</h2><h3 id="traditional-client-side-rendering" tabindex="-1"><a class="header-anchor" href="#traditional-client-side-rendering" aria-hidden="true">#</a> Traditional client-side rendering:</h3><ol><li>the server sends a minimal HTML skeleton to the client (div with an id for the React or Angular app to amount)</li><li>the client then downloads the JavaScript bundle</li><li>once the JavaScript is loaded, the framework takes over and renders the full page content, updating the DOM</li></ol><h3 id="server-side-rendering" tabindex="-1"><a class="header-anchor" href="#server-side-rendering" aria-hidden="true">#</a> Server-side rendering</h3><ol><li>the server generates the initial HTML content and sends it to the client.</li><li>the client can start rendering the page immediately, while the JavaScript files are still downloading in the background.</li><li>Once the JavaScript is loaded, the framework can then hydrate the page, taking over the rendering and make the page interactive.</li></ol><h2 id="css-styling" tabindex="-1"><a class="header-anchor" href="#css-styling" aria-hidden="true">#</a> CSS Styling</h2><h3 id="global-styles" tabindex="-1"><a class="header-anchor" href="#global-styles" aria-hidden="true">#</a> Global Styles</h3><p>If we look inside the <code>/app/ui</code> folder, we&#39;ll see a file called <code>global.css</code>. We can use this file to add CSS rules to all the routes in the application.</p><h3 id="tailwind" tabindex="-1"><a class="header-anchor" href="#tailwind" aria-hidden="true">#</a> Tailwind</h3><p>Tailwind is a CSS framework that speeds up the development process by allowing us to quickly write utility classes directly in our TSX markup.</p><h2 id="creating-layout-and-pages" tabindex="-1"><a class="header-anchor" href="#creating-layout-and-pages" aria-hidden="true">#</a> Creating layout and pages</h2><h3 id="nested-routing" tabindex="-1"><a class="header-anchor" href="#nested-routing" aria-hidden="true">#</a> Nested Routing</h3><p>Next.js uses file-system routing where folders are used to create nested routes. Each folder represents a route segment that maps to a URL segment</p><figure><img src="'+t+'" alt="nested routing" tabindex="0" loading="lazy"><figcaption>nested routing</figcaption></figure><p><code>page.tsx</code> is a special Next.js file that exports a React component, and it&#39;s required for the route to be accessible. In our application, we already have a page file: <code>/app/page.tsx</code> - this is the home page associated with the route <code>/</code>.</p><p>To create a nested route, we can nest folders inside each other and add <code>page.tsx</code> files inside them. <img src="'+i+`" alt="nested index page" loading="lazy"></p><h2 id="setting-up-database" tabindex="-1"><a class="header-anchor" href="#setting-up-database" aria-hidden="true">#</a> Setting Up Database</h2><h3 id="vercel" tabindex="-1"><a class="header-anchor" href="#vercel" aria-hidden="true">#</a> Vercel</h3><p>Vercel is a cloud platform for static sites and serverless functions. It&#39;s primarily used for hosting and deploying Next.js applications, but it can also be used with other frameworks and static site generators like React, Vue.js, Angular.</p><ol><li><p><strong>Hosting</strong>: Vercel provides a cloud based hosting platform for our web applications, making it easy to deploy and manage our projects.</p></li><li><p><strong>Serverless Functions</strong>: Vercel supports serverless functions, which allow us to run backend code on the server without the need to manage a full server infrastructure.</p></li><li><p><strong>Automatic Deployments</strong>: Vercel integrates with Git repositories and automatically deploys our application whenever we push changes to our codebase.</p></li><li><p><strong>Global CDN</strong>: Vercel&#39;s infrastructure is powered by a global Content Delivery Network(CDN), which ensures fast and reliable delivery of our web application.</p></li></ol><h3 id="database-seeding" tabindex="-1"><a class="header-anchor" href="#database-seeding" aria-hidden="true">#</a> Database seeding:</h3><p>Database seeding is the process of populating a database with an initial set of data. This data can be used for testing purposes, or to provide a set of known data that an application can rely on when it first runs.</p><h2 id="choosing-how-to-fetch-data" tabindex="-1"><a class="header-anchor" href="#choosing-how-to-fetch-data" aria-hidden="true">#</a> Choosing how to fetch data</h2><h3 id="api-layer" tabindex="-1"><a class="header-anchor" href="#api-layer" aria-hidden="true">#</a> API layer</h3><p>APIs are intermediary layer between our application code and database. There are a few cases where we might use an API:</p><ul><li>If we are using 3rd party services that provide an API</li><li>If we are fetching data from the client, we want to have an API layer that runs on the server to avoid exposing our database secrets to the client</li></ul><h3 id="using-server-components-to-fetch-data" tabindex="-1"><a class="header-anchor" href="#using-server-components-to-fetch-data" aria-hidden="true">#</a> Using Server Components to fetch data</h3><p>By default, Next.js applications use <strong>React Server Components</strong>. Fetching data with Server Componnts is a relatively new approach and there are a few benefits of using them:</p><ul><li>Server Components support promises, providing a simpler solution for asynchronous tasks like data fetching. We can use <code>async/await</code> syntax without reaching out for <code>useEffect</code>, <code>useState</code> or data fetching libraries..</li><li>Server Components execute on the server, so we can keep expensive data and logic on the server and only send the result to the client.</li><li>As mentioned before, since Server Components execute on the server, we can query the database directly without an additional API layer.</li></ul><h3 id="using-sql" tabindex="-1"><a class="header-anchor" href="#using-sql" aria-hidden="true">#</a> Using SQL</h3><p>Go to <code>/app/lib/data.ts</code>, we&#39;ll see that we are importing the <code>sql</code> function from <code>@vercel/postgres</code>. This function allows us to query our database:</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> sql <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;@vervel/postgres&quot;</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>We can call <code>sql</code> inside any Server Component. But to allow us to navigate the components more easily, we&#39;ve kept all the data queries in the <code>data.ts</code> file, and we can import them into the components.</p><h3 id="what-are-request-waterfalls" tabindex="-1"><a class="header-anchor" href="#what-are-request-waterfalls" aria-hidden="true">#</a> What are request waterfalls?</h3><p>A &quot;waterfall&quot; refers to a sequence of network requests that depend on the completion of previous requests. In the case of data fetching, each request can only begin once the previous request has returned data.</p><p>For example, we need to wait for <code>fetchRevenue()</code> to execute before <code>fetchLatestInvoices()</code> can start running, and so on.</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">const</span> revenue <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetchRevenue</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> latestInvoices <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetchLatestInvoices</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// wait for fetchRevenue() to finish</span>
<span class="token keyword">const</span> <span class="token punctuation">{</span>
  numberOfInvoices<span class="token punctuation">,</span>
  numberOfCustomers<span class="token punctuation">,</span>
  totalPaidInvoices<span class="token punctuation">,</span>
  totalPendingInvoices<span class="token punctuation">,</span>
<span class="token punctuation">}</span> <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">fetchCardData</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// wait for fetchLatestInvoices() to finish</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>This pattern is not necessarily bad. There may be cases where we want waterfalls because we want a condition to be satisfied before we make the next request. However, this behavior can also be unintentional and impact performance.</p><h3 id="parallel-data-fetching" tabindex="-1"><a class="header-anchor" href="#parallel-data-fetching" aria-hidden="true">#</a> Parallel data fetching</h3><p>A common way to avoid waterfalls is to initiate all data requests at the same time - in parallel.</p><p>In JavaScript, we can use the <code>Promise.all()</code> or <code>Promise.allSettled()</code> functions to initiate all promises at the same time.</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchCardData</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">try</span> <span class="token punctuation">{</span>
    <span class="token keyword">const</span> invoiceCountPromise <span class="token operator">=</span> sql<span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">SELECT COUNT(*) FROM invoices</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">;</span>
    <span class="token keyword">const</span> customerCountPromise <span class="token operator">=</span> sql<span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">SELECT COUNT(*) FROM customers</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">;</span>
    <span class="token keyword">const</span> invoiceStatusPromise <span class="token operator">=</span> sql<span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">SELECT
         SUM(CASE WHEN status = &#39;paid&#39; THEN amount ELSE 0 END) AS &quot;paid&quot;,
         SUM(CASE WHEN status = &#39;pending&#39; THEN amount ELSE 0 END) AS &quot;pending&quot;
         FROM invoices</span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">;</span>

    <span class="token keyword">const</span> data <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token builtin">Promise</span><span class="token punctuation">.</span><span class="token function">all</span><span class="token punctuation">(</span><span class="token punctuation">[</span>
      invoiceCountPromise<span class="token punctuation">,</span>
      customerCountPromise<span class="token punctuation">,</span>
      invoiceStatusPromise<span class="token punctuation">,</span>
    <span class="token punctuation">]</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token comment">// ...</span>
  <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>By using this pattern, we can:</p><ul><li>Start executing all data fetches at the same time, which can lead to performance gains.</li><li>Use a native JavaScript that can be applied to any library or framework.</li></ul><h2 id="static-rendering" tabindex="-1"><a class="header-anchor" href="#static-rendering" aria-hidden="true">#</a> Static Rendering:</h2><p>With static rendering, data fetching and rendering happens on the server at build time (when we deploy) or during revalidation. The result can then be distributed and cached in a CDN.</p><p>Whenever a user visits our application, the cached result is served. There are a couple of benefits of static rendering:</p><ol><li><strong>Faster Websites</strong>: Prerendered content can be cached and globally distributed. This ensures that users around the world can access our website&#39;s content more quickly and reliably.</li><li><strong>Reduced Server Load</strong>: Because the content is cached, our server does not have to dynamically generate content for each user request.</li><li><strong>SEO</strong>: Prerendered content is easier for search engine crawlers to index, as the content is already available when the page loads. This can lead to improved search engine rankings.</li></ol><p>Static rendering is useful for UI with <strong>no data</strong> or <strong>data that is shared across users</strong>, such as a static blog post or a product page. It might not be a good fit for a dashboard that has personalized data that is regularly updated.</p><h2 id="dynamic-rendering" tabindex="-1"><a class="header-anchor" href="#dynamic-rendering" aria-hidden="true">#</a> Dynamic Rendering:</h2><p>With dynamic rendering, content is rendered on the server for each user at <strong>request time</strong>(when the user visits the page). There are a couple of benefits of dynamic rendering:</p><ul><li><strong>Real-Time Data</strong>: Dynamic rendering allows our application to display real-time or frequently updated data. This is idea for applications where data changes often.</li><li><strong>User-Specific Content</strong>: It&#39;s easier to serve personalized content, such as dashboards or user profiles, and update the data based on user interaction.</li><li><strong>Request Time Information</strong>: Dynamic rendering allows us to access information that can be known at request time, such as cookies or the URL search parameters.</li></ul><h3 id="making-the-dashboard-dynamic" tabindex="-1"><a class="header-anchor" href="#making-the-dashboard-dynamic" aria-hidden="true">#</a> Making the dashboard dynamic</h3><p>By default, <code>@vercel/postgres</code> doesn&#39;t set its own caching semantics. This allows the framework to set its own static and dynamic behavior.</p><p>We can use a Next.js API called <code>unstable_noStore</code> inside our Server Components or data fetching functions to opt out of static rendering. Let&#39;s add this.</p><p>In our <code>data.ts</code>, import <code>unstable_noStore</code> from <code>next/cache</code>, and call it the top of our data fetching functions:</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token comment">// ...</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> unstable_noStore <span class="token keyword">as</span> noStore <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;next/cache&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchRevenue</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token comment">// Add noStore() here to prevent the response from being cached.</span>
  <span class="token comment">// This is equivalent to in fetch(..., {cache: &#39;no-store&#39;}).</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchLatestInvoices</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchCardData</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchFilteredInvoices</span><span class="token punctuation">(</span>
  query<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">,</span>
  currentPage<span class="token operator">:</span> <span class="token builtin">number</span>
<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchInvoicesPages</span><span class="token punctuation">(</span>query<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchFilteredCustomers</span><span class="token punctuation">(</span>query<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">fetchInvoiceById</span><span class="token punctuation">(</span>query<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token function">noStore</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token comment">// ...</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="streaming" tabindex="-1"><a class="header-anchor" href="#streaming" aria-hidden="true">#</a> Streaming</h2><p>Streaming is a data transfer technique that allows us to break down a route into smaller &quot;chunks&quot; and progressively stream them from the server to the client as they become ready.</p><p>By streaming, we can prevent slow data requests from blocking our whole page. This allows the user to see and interact with parts of the page without waiting for all the data to load before any UI can be shown to the user.</p><p>There are two ways we implement streaming in Next.js:</p><ol><li>At the page level, with the <code>loading.tsx</code> file. <ul><li><code>loading.tsx</code> is a special Next.js file built on top of Suspense, it allows us to create fallback UI to show as a replacement while page content loads.</li><li>Since <code>&lt;SideNav&gt;</code> is static, it&#39;s shown immediately. The user can interact with <code>&lt;SideNav&gt;</code> while the dynamic content is loading.</li><li>The user doesn&#39;t have to wait for the page to finish loading before navigating away (this is called interriptable navigation).</li></ul></li><li>For specific components, with <code>&lt;Suspense&gt;</code>.</li></ol><h3 id="streaming-a-component" tabindex="-1"><a class="header-anchor" href="#streaming-a-component" aria-hidden="true">#</a> Streaming a component</h3><p>So far, we are streaming a whole page. But,instead, we can be more granular and stream specific components using React Suspense.</p><p>Suspense allows us to defer rendering parts of our application until some condition is met(e.g. data is loaded). We can wrap our dynamic components in Suspense. Then, pass it a fallback component to show while the dynamic component loads.</p><h2 id="adding-search-and-pagination" tabindex="-1"><a class="header-anchor" href="#adding-search-and-pagination" aria-hidden="true">#</a> Adding Search and Pagination</h2><h3 id="adding-the-search-functionality" tabindex="-1"><a class="header-anchor" href="#adding-the-search-functionality" aria-hidden="true">#</a> Adding the search functionality</h3><p>These are the Next.js client hooks that we&#39;ll use to implement the search functionality:</p><ol><li><code>useSearchParams</code> - Allows us to access the params of the current URL. For example, the search params for this URL <code>/dashboard/invoices?page=1&amp;query=pending</code> would look like this: <code>{page: &#39;1&#39;, query: &#39;pending&#39;}</code>.</li><li><code>usePathname</code> - Let us read the current URL&#39;s pathname. For example, for the route <code>/dashboard/invoices</code>, <code>usePathname</code> would return <code>/dashboard/invoices</code>.</li><li><code>useRouter</code> - Enables navigation between routes within client components programmatically.</li></ol><p>Here&#39;s the quick overview of the implementation steps:</p><ol><li>Capture the user&#39;s input</li><li>Update the URL with the search params</li><li>Keep the URL in sync with the input field</li><li>Update the table to reflect the search query</li></ol><h3 id="debouncing" tabindex="-1"><a class="header-anchor" href="#debouncing" aria-hidden="true">#</a> Debouncing</h3><p><strong>Debouncing</strong> is a programming practice that limits the rate at which a function can fire. In our case, we only want to query the database when the user has stopped typing.</p><p>Here&#39;s how debouncing works:</p><ol><li><strong>Trigger Event</strong>: When an event that should be debounced(like a keystroke in the search box)occurs, a timer starts.</li><li><strong>Wait</strong>: If a new event occurs before the timer expires, the timer is reset.</li><li><strong>Execution</strong>: If a timer reaches the end of its countdown, the debounced function is executed.</li></ol><h2 id="mutating-data" tabindex="-1"><a class="header-anchor" href="#mutating-data" aria-hidden="true">#</a> Mutating Data</h2><h3 id="what-are-server-actions" tabindex="-1"><a class="header-anchor" href="#what-are-server-actions" aria-hidden="true">#</a> What are Server Actions?</h3><p>React Server Actions allow us to run asynchronous code directly on the server. They eliminate the need to create API endpoints to mutate our data. Instead, we write asynchronous functions that execute on the server and can be invoked from our Client or Server Components.</p><h3 id="use-server-directive" tabindex="-1"><a class="header-anchor" href="#use-server-directive" aria-hidden="true">#</a> <code>use server</code> directive</h3><p>The <code>use server</code> directive is part of the Next/js Server Components feature, which allows us to define components that will be rendered on the server side.</p><h3 id="create-a-server-action" tabindex="-1"><a class="header-anchor" href="#create-a-server-action" aria-hidden="true">#</a> Create a Server Action</h3><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token punctuation">{</span> customerField <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;@/app/lib/definitions&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> Link <span class="token keyword">from</span> <span class="token string">&#39;next/link&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span>
  CheckIcon<span class="token punctuation">,</span>
  ClockIcon<span class="token punctuation">,</span>
  CurrencyDollarIcon<span class="token punctuation">,</span>
  UserCircleIcon<span class="token punctuation">,</span>
<span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;@heroicons/react/24/outline&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> Button <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;@/app/ui/button&#39;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> createInvoice <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&#39;@/app/lib/actions&#39;</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">default</span> <span class="token keyword">function</span> <span class="token function">Form</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
  customers<span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token operator">:</span> <span class="token punctuation">{</span>
  customers<span class="token operator">:</span> customerField<span class="token punctuation">[</span><span class="token punctuation">]</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">return</span> <span class="token punctuation">(</span>
    <span class="token operator">&lt;</span>form action<span class="token operator">=</span><span class="token punctuation">{</span>createInvoice<span class="token punctuation">}</span><span class="token operator">&gt;</span>
      <span class="token comment">// ...</span>
  <span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>In HTML, we&#39;d pass a URL to the <code>action</code> attribute. This URL would be the destination where our form data should be submitted (usually an API endpoint).</p><p>However, in React, the <code>action</code> attribute is considered a special prop - meaning React builds on top of it to allow actions to be invoked.</p><p>Behind the scenes, Server Actions create a <code>POST</code> API endpoint. This is why we don&#39;t need to create API endpoints menually when using Server Actions.</p><h3 id="extract-the-data-from-formdata" tabindex="-1"><a class="header-anchor" href="#extract-the-data-from-formdata" aria-hidden="true">#</a> Extract the data from <code>formData</code></h3><p>Back in our <code>action.ts</code> file, we&#39;ll need to extract the values of <code>formData</code>, there are a couple of methods we can use. For this example, let&#39;s use the <code>.get(name)</code> method.</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token string">&quot;use server&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">createInvoice</span><span class="token punctuation">(</span>formData<span class="token operator">:</span> FormData<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> rawFormData <span class="token operator">=</span> <span class="token punctuation">{</span>
    customerId<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;customerId&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    amount<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;amount&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    status<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;status&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">;</span>
  <span class="token comment">// Test it out:</span>
  <span class="token builtin">console</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span>rawFormData<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>createInvoice</code> here is a Server Action. We can think of it as Server Actions Post API in backend.</p><h3 id="validating-and-prepare-the-data" tabindex="-1"><a class="header-anchor" href="#validating-and-prepare-the-data" aria-hidden="true">#</a> Validating and prepare the data</h3><p>Before sending the form data to our database, we want to ensure it&#39;s in the correct format and with the correct types. For example:</p><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">export</span> <span class="token keyword">type</span> <span class="token class-name">Invoice</span> <span class="token operator">=</span> <span class="token punctuation">{</span>
  id<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span> <span class="token comment">// Will be created on the database</span>
  customer_id<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
  amount<span class="token operator">:</span> <span class="token builtin">number</span><span class="token punctuation">;</span> <span class="token comment">// Stored in cents</span>
  status<span class="token operator">:</span> <span class="token string">&quot;pending&quot;</span> <span class="token operator">|</span> <span class="token string">&quot;paid&quot;</span><span class="token punctuation">;</span>
  date<span class="token operator">:</span> <span class="token builtin">string</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="type-validation-and-coercion" tabindex="-1"><a class="header-anchor" href="#type-validation-and-coercion" aria-hidden="true">#</a> Type validation and coercion:</h3><p>It&#39;s important to validate that the data from our form aligns with the expected types in our database.</p><p>To handle type validation, we can use Zod, a Typescript-first validation library that can simplify task for us.</p><h3 id="next-js-client-side-router-cache" tabindex="-1"><a class="header-anchor" href="#next-js-client-side-router-cache" aria-hidden="true">#</a> Next.JS client-side router cache</h3><ul><li>Next.js uses a client-side router cache to improve the performance of navigation between pages</li><li>When a user navigates to a new page, Next.js stores the rendered page in the client-side cache</li><li>On subsequent visits to the same page, Next.js can retrieve the cached version, which can significantly improve the laod time, as the page doesn&#39;t need to be re-rendered from the server.</li></ul><h3 id="revalidate-and-redirect" tabindex="-1"><a class="header-anchor" href="#revalidate-and-redirect" aria-hidden="true">#</a> Revalidate and redirect</h3><ul><li><code>revalidatePath</code> is a feature in Next.js that allows us to manually invalidate the cache for a specific page or set of pages.</li><li>When we call <code>revalidatePath</code>, we are telling Next.js to invalidate the cache for the specified path(s) and fetch the latest data from the server.</li></ul><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token string">&quot;use server&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">import</span> <span class="token punctuation">{</span> z <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;zod&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> sql <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;@vercel/postgres&quot;</span><span class="token punctuation">;</span>
<span class="token keyword">import</span> <span class="token punctuation">{</span> revalidatePath <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;next/cache&quot;</span><span class="token punctuation">;</span>

<span class="token comment">// ...</span>

<span class="token keyword">export</span> <span class="token keyword">async</span> <span class="token keyword">function</span> <span class="token function">createInvoice</span><span class="token punctuation">(</span>formData<span class="token operator">:</span> FormData<span class="token punctuation">)</span> <span class="token punctuation">{</span>
  <span class="token keyword">const</span> <span class="token punctuation">{</span> customerId<span class="token punctuation">,</span> amount<span class="token punctuation">,</span> status <span class="token punctuation">}</span> <span class="token operator">=</span> CreateInvoice<span class="token punctuation">.</span><span class="token function">parse</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
    customerId<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;customerId&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    amount<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;amount&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
    status<span class="token operator">:</span> formData<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&quot;status&quot;</span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token keyword">const</span> amountInCents <span class="token operator">=</span> amount <span class="token operator">*</span> <span class="token number">100</span><span class="token punctuation">;</span>
  <span class="token keyword">const</span> date <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Date</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">toISOString</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">split</span><span class="token punctuation">(</span><span class="token string">&quot;T&quot;</span><span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token punctuation">]</span><span class="token punctuation">;</span>

  <span class="token keyword">await</span> sql<span class="token template-string"><span class="token template-punctuation string">\`</span><span class="token string">
    INSERT INTO invoices (customer_id, amount, status, date)
    VALUES (</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span>customerId<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">, </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span>amountInCents<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">, </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span>status<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">, </span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">\${</span>date<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">)
  </span><span class="token template-punctuation string">\`</span></span><span class="token punctuation">;</span>

  <span class="token function">revalidatePath</span><span class="token punctuation">(</span><span class="token string">&quot;/dashboard/invoices&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
  <span class="token function">redirect</span><span class="token punctuation">(</span><span class="token string">&quot;/dashboard/invoices&quot;</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="accessibility" tabindex="-1"><a class="header-anchor" href="#accessibility" aria-hidden="true">#</a> Accessibility</h2><p>Accessibility means building web pages and applications that are usable by as many people as possible, including those who use assistive technologies like screen readers.</p><h3 id="using-the-eslint-accessibility-plugin-in-next-js" tabindex="-1"><a class="header-anchor" href="#using-the-eslint-accessibility-plugin-in-next-js" aria-hidden="true">#</a> Using the ESLint accessibility plugin in Next.js</h3><ul><li>Add <code>next lint</code> as a script in our package.json file</li><li>Run <code>npm run lint</code> in our terminal</li></ul><h2 id="adding-authentication" tabindex="-1"><a class="header-anchor" href="#adding-authentication" aria-hidden="true">#</a> Adding Authentication</h2><h3 id="authentication-vs-authorization" tabindex="-1"><a class="header-anchor" href="#authentication-vs-authorization" aria-hidden="true">#</a> Authentication vs. Authorization</h3><ul><li><strong>Authentication</strong> is about making sure the user is who they say they are. You&#39;re proving your identity with something you have like a suername and password.</li><li><strong>Authorization</strong> is the next step. Once a user&#39;s identity is confirmed, authorization decides what parts of the application they are allowed to use.</li></ul><p>So, authentication checks who you are, and authorization determines what you can do or access in the application.</p><h3 id="nextauth-js" tabindex="-1"><a class="header-anchor" href="#nextauth-js" aria-hidden="true">#</a> NextAuth.js</h3><p>NextAuth.js is an open-source library for Next.js that makes it easy to add authentication to our Next.js applications. It supports various authentication strategies, including OAuth, and JWT, among others.</p><ol><li><strong>Setting up NextAuth.js</strong> Install NextAuth.js by running the following command in our terminal:</li></ol><div class="language-terminal line-numbers-mode" data-ext="terminal"><pre class="language-terminal"><code>npm install next-auth@beta
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Next, generate a secret key for our application. This key is used to encrypt cookies, ensuring the security of user sessions. We can do this by running the following command in our terminal:</p><div class="language-terminal line-numbers-mode" data-ext="terminal"><pre class="language-terminal"><code>openssl rand -base64 32
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then, in our <code>.env</code> file, add our generated key to the <code>AUTH_SECRET</code> variable:</p><div class="language-.env line-numbers-mode" data-ext=".env"><pre class="language-.env"><code>AUTH_SECRET=your-secret-key
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ol start="2"><li>Adding the pages option Create an <code>auth.config.ts</code> file at the root of our project that exports an <code>authConfig</code> object. This object will contain the configuration options for NextAuth.js. For now, it will only contain the <code>pages</code> option:</li></ol><div class="language-typescript line-numbers-mode" data-ext="ts"><pre class="language-typescript"><code><span class="token keyword">import</span> <span class="token keyword">type</span> <span class="token punctuation">{</span> NextAuthConfig <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">&quot;next-auth&quot;</span><span class="token punctuation">;</span>

<span class="token keyword">export</span> <span class="token keyword">const</span> authConfig <span class="token operator">=</span> <span class="token punctuation">{</span>
  pages<span class="token operator">:</span> <span class="token punctuation">{</span>
    signIn<span class="token operator">:</span> <span class="token string">&quot;/login&quot;</span><span class="token punctuation">,</span>
  <span class="token punctuation">}</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,123),p=[c];function r(l,d){return a(),s("div",null,p)}const h=n(o,[["render",r],["__file","nextjs.html.vue"]]);export{h as default};
